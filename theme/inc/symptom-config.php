<?php
/**
 * 症状ページ 共通設定
 *
 * 院の情報とリンク先をここで一元管理します。
 * この1ファイルを直せば、すべての症状ページに反映されます。
 *
 * ★ 空文字（''）にした項目は、ページ上に自動で表示されません。
 *   住所・電話・受付時間・料金は「変更の可能性あり」とのご指定により、
 *   現在すべて非表示（空欄）にしてあります。
 *   掲載したくなったタイミングで値を入れれば、そのまま表示されます。
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

return array(

	/* ------------------------------------------------------------------
	 * テーマ連携
	 *
	 * content_class …… 症状ページ全体を囲むCSSクラス。
	 *   多くのテーマ（TCD製テーマを含む）は、見出し・段落・リストの書式を
	 *   「本文エリアのクラスの中だけ」に当てています。そのクラスをここに指定すると、
	 *   症状ページにもテーマ本来の書式がそのまま適用され、他ページと揃います。
	 *
	 *   ▼調べ方
	 *     既存の固定ページをブラウザで開き、本文を右クリック →「検証」。
	 *     本文全体を囲んでいる div の class（例: post_content / entry-content）を確認。
	 *
	 *   空文字にすると、テーマの本文スタイルを一切継承しない素の状態になります。
	 * ---------------------------------------------------------------- */
	'theme' => array(
		'content_class' => 'post_content',   // ★要確認（TCDテーマの標準的なクラス名を初期値にしています）

		/*
		 * style_mode …… 見た目をどこまで自前で持つか。
		 *
		 *   'plain'  … 独自CSSを一切読み込まず、素のHTML（h2 / p / ul / ol / dl）だけを出力。
		 *              見出しもボタンも余白も、すべてテーマのデザインがそのまま出ます。
		 *              他ページとの統一感は最も高くなります。【初期値・推奨】
		 *
		 *   'styled' … 付属の symptom.css を読み込み、カードやステップなどの
		 *              箱組みを付けます。読ませる工夫は増えますが、
		 *              テーマとの馴染みは 'plain' に劣ります。
		 *
		 * まず 'plain' で公開し、物足りなければ 'styled' に変えて見比べるのが安全です。
		 */
		'style_mode' => 'plain',
	),

	/* ------------------------------------------------------------------
	 * 院の基本情報
	 * ※現在は院名のみ表示。他は空欄＝非表示にしています。
	 * ---------------------------------------------------------------- */
	'clinic' => array(
		'name'    => 'ABCカイロプラクティック整体院',
		'area'    => '新潟市',
		'address' => '',   // 空欄＝非表示（掲載する場合のみ入力）
		'access'  => '',   // 空欄＝非表示
		'hours'   => '',   // 空欄＝非表示
		'holiday' => '',   // 空欄＝非表示
	),

	/* ------------------------------------------------------------------
	 * 予約導線（⑦行動セクションのボタン）
	 * URL を空にしたボタンは表示されません。
	 * 現在は LINE のみを導線としています。
	 * ---------------------------------------------------------------- */
	'cta' => array(
		'line_url'    => 'https://lin.ee/VG1WL3J',
		'line_note'   => '24時間受付・ご相談だけでも大丈夫です',

		'tel'         => '',   // 空欄＝電話ボタン非表示
		'tel_note'    => '',
		'web_url'     => '',   // 空欄＝Web予約ボタン非表示
		'web_note'    => '',
		'first_price' => '',   // 空欄＝料金表示なし
		'price_note'  => '',
	),

	/* ------------------------------------------------------------------
	 * 内部リンク先（⑤実例・⑥専門性セクション）
	 * 相対パス（/case/）でも絶対URLでも指定できます。
	 * ---------------------------------------------------------------- */
	'links' => array(
		'case'    => '/case/',        // ★要確認 症例ページの実際のURLに合わせてください
		'posture' => '/posture/',     // ★要確認 姿勢改善ページの実際のURLに合わせてください
	),

	/* ------------------------------------------------------------------
	 * 免責・注意書き（薬機法・景表法対策）
	 * すべての症状ページの末尾に共通表示されます。
	 * ---------------------------------------------------------------- */
	'disclaimer' => '当院は医療機関ではありません。施術は治療・診断を目的とするものではなく、効果の感じ方や必要な回数には個人差があります。強い痛みやしびれ、発熱をともなう場合は、まず医療機関の受診をおすすめします。',
);
